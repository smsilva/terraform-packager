#!/bin/bash
TERRAFORM_VERSION_COMPATIBLE_WITH_REFRESH_ONLY="1.2.0"
TERRAFORM_VERSION_LATEST=$(latest_version ${TERRAFORM_VERSION_COMPATIBLE_WITH_REFRESH_ONLY} ${TERRAFORM_VERSION})

if [ "${TERRAFORM_VERSION}" == "${TERRAFORM_VERSION_LATEST}" ]; then
  echo y
else
  echo n
fi
