apiVersion: v1
kind: Pod
metadata:
  name: azure-fake-module
  namespace: default
spec:
  containers:
  - name: terraform
    image: silviosilva/azure-fake-module:latest
    args: ["plan", "-no-color"]
    env:
    - name: DEBUG
      value: "0"
    envFrom:
    - secretRef:
        name: arm-credentials
  restartPolicy: OnFailure
